/// <reference types="react" />
import { StoreType } from '../model/store';
declare type Props = {
    store: StoreType;
    hideSvgEffects?: boolean;
};
export declare const SvgToolbar: (({ store, hideSvgEffects }: Props) => JSX.Element) & {
    displayName: string;
};
export default SvgToolbar;
